# Current Request

## Raw User Request

Tengo un problema, actualmente el front me comunica POST http://localhost:3000/venues 403 (Forbidden) al consultar el back, pero no queremos que se configure aún credenciales, elimina la autenticación o lo que sea que provoque esto
